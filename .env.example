# Optional — okapi works with your local Claude Code login by default.
# Set this only to bill through the Anthropic API instead; when set, it
# takes precedence over the local login.
# Get a key at https://platform.claude.com/
ANTHROPIC_API_KEY=
